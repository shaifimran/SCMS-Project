// API URL - would come from environment variables in real project
export const API_URL = 'https://api.scms.example.com/api/v1';

// Complaint status options
export const COMPLAINT_STATUS = {
  PENDING: 'Pending',
  IN_PROGRESS: 'In Progress',
  RESOLVED: 'Resolved',
};

// Complaint priority options
export const COMPLAINT_PRIORITY = {
  LOW: 'Low',
  MEDIUM: 'Medium',
  HIGH: 'High',
};

// User roles
export const USER_ROLES = {
  USER: 'User',
  STAFF: 'Staff',
  ADMIN: 'Admin',
};

// Verification status options
export const VERIFICATION_STATUS = {
  PENDING: 'Pending',
  VERIFIED: 'Verified',
  REJECTED: 'Rejected',
};